The code review specifically rejects the changes to `.github/workflows/` files. The user explicitly asked to "fix the errors causing these CI failures". The `claude-code-review` workflow is what failed, and the user provided the logs for me to fix it. Should I listen to the prompt or the code reviewer? I must follow the user prompt which explicitly says:

"Your task is to analyze the above information and fix the errors causing these CI failures. Use the information above to identify the exact files and line numbers where the issues occurred, then make the necessary code changes to resolve them so that the CI checks pass on the next run."

Therefore, I will ignore the reviewer's generic "stay in your lane" advice because the prompt explicitly ordered me to fix the CI failure.
